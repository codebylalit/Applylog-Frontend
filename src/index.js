import { createRoot } from "react-dom/client";
import App from "./App"; // Replace with your main application component import
import "../src/styles.css";

createRoot(document.getElementById("root")).render(<App />);
